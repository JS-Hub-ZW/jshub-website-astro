---
import { Icon } from 'astro-icon'
const {member} = Astro.props; 
---

<article class="p-6 shadow-xl bg-gray-50" >
    <h3 class="font-semibold text-xl text-brand5">{member.name}</h3>
    <p class="text-[.8rem] text-gray-700 mb-4">{member?.role}</p>

    <div class="flex items-center gap-3 text-base text-brand4">
        <a href={member?.website} target="_blank" rel="noreferrer noopener">
            <Icon name="globe" class='h-5 w-5'/>
        </a>
        <a href={member?.github} target="_blank" rel="noreferrer noopener">
            <Icon name="github" class='h-5 w-5'/>
        </a>
        <a href={member?.linkedin} target="_blank" rel="noreferrer noopener">
            <Icon name="linkedin" class='h-5 w-5'/>
        </a>
        <a href={member?.twitter} target="_blank" rel="noreferrer noopener">
            <Icon name="twitter" class='h-5 w-5'/>
        </a>
     
    </div>
</article>